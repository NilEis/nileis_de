---
const version = await fetch("http://localhost:4321/api/version.json").then(
  (res) => res.json()
);

interface Props {
  prefix?: string;
}

const { prefix = "hash:" } = Astro.props;
export const prerender = true;
---

<code id="hash">{prefix} {version.hash} - {version.name}</code>
