---
import TitleAndMeta from "../../components/TitleAndMeta.astro";
import LinkList from "../../components/LinkList.astro";
import MainContent from "../../components/MainContent.astro";
import { path_routes, type Route } from "../../data/routes";
const sites = path_routes.get("ai/")?.children?.sort((a, b) => a.name.localeCompare(b.name)) || new Array<Route>();
---

<TitleAndMeta title="AI" />
<MainContent>
  <LinkList sites={sites} outerClass="h-screen" />
</MainContent>
